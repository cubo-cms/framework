{
  "/": {
    "regex": "^\\/$",
    "controller": "Document"
  },
  "/{controller}": {
    "regex": "^\\/([^/:]+)$",
    "controller": "{controller}"
  },
  "/{controller}:{name}": {
    "regex": "^\\/([^/:]+):([^/:]+)$",
    "controller": "{controller}",
    "name": "{name}"
  }
}
